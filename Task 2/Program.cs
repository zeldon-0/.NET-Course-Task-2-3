using System;
using TreeImplementation;
using System.Collections.Generic;
using System.Linq;
namespace Task_2
{
    class Program
    {
    
        static void Main(string[] args)
        {
            /* 
            Tree<ExamResult> a=new Tree<ExamResult>() {
                new ExamResult(50, "Sam", new DateTime(1,1,1)),
                new ExamResult(70, "Bob", new DateTime(1,1,1)),
                new ExamResult(80, "John", new DateTime(2000,1,1)),
                new ExamResult(20, "Stephen",new DateTime(1,1,1)),
                new ExamResult(60, "George"),
                new ExamResult(34, "Kate")};
            foreach(var value in a.PreOrder())
                Console.WriteLine(value);

            a.Alert+= ((object o, TreeChangeEventArgs e)=>Console.WriteLine(e.Info));

            a.Delete(new ExamResult(50, "Sam", new DateTime(1,1,1)));

            foreach(var value in a.PreOrder())
                Console.WriteLine(value);
            */
            /*IntervalArray<int> array= new IntervalArray<int>(){1,2,3,4,5,6};
            foreach (var i in array)
            {
                Console.WriKteLine(i);
            }*/
        }
    }
}
